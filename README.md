# Mini Chat Bar

一个基于 Vue.js + Node.js + Socket.IO 的实时聊天应用，支持 Web 端和 Electron 桌面端。

## 项目简介

这是一个功能完整的即时通讯应用，支持一对一私聊、多人聊天室、文件传输、语音消息等功能。项目采用前后端分离架构，使用 WebSocket 实现实时通信，并集成了 AI 助手功能。

### 核心功能

- 💬 实时聊天：支持文字、图片、语音、文件等多种消息类型
- 👥 聊天室：创建和加入多人聊天室，支持实时在线状态
- 🤖 AI 助手：集成智能对话功能
- 📱 跨平台：支持 Web 浏览器和 Electron 桌面应用
- 🔐 用户认证：完整的注册登录系统，支持第三方登录
- 📎 文件传输：支持图片、文件上传和分享
- 🎨 动态背景：基于 Canvas 的动态粒子效果

## 开发心得

### 从零到一的探索

这个项目是我从"想做一个聊天软件"到"真的做出来了"的完整历程。最开始只是想模仿一下市面上的聊天工具，但很快发现自己的能力还不足以完全复刻，于是决定做一个原创的、简化版的聊天室。

### 技术选型的纠结

一开始在数据库选择上纠结了很久，最终选择了 MongoDB，主要是因为它的文档型结构更适合存储聊天消息这种非结构化数据。Socket.IO 的选择则是因为它封装得很好，对新手友好，虽然后来发现有些场景下原生 WebSocket 可能更合适。

### 踩过的坑

1. **文件传输**：最初想通过 Socket.IO 直接传输文件，结果发现大文件会导致连接阻塞。后来改用 HTTP 上传 + Socket 通知的方案，但还没完全优化好。

2. **状态同步**：多个客户端之间的状态同步是个大问题，尤其是在线状态和未读消息数。最后用了 Pinia 做状态管理，配合 Socket 事件才勉强搞定。

3. **Electron 打包**：打包 Electron 应用时遇到了各种路径问题，开发环境能跑，打包后就白屏。最后发现是静态资源路径没处理好，花了好几天才解决。

### 未完成的遗憾

项目还有很多想做但没做完的功能：
- 匿名聊天模式（数据库字段都留好了，就是没时间实现）
- 完整的文件传输系统（目前只是半成品）
- 朋友圈功能（画了原型图，代码还没开始写）
- 更好的ai探索
- 结合上openClaw会咋样呢？

### 收获与感悟

虽然项目不完美，bug 也不少，但能跑起来就是胜利。通过这个项目，我对前后端交互、实时通信、状态管理有了更深的理解。最重要的是，我学会了如何在能力有限的情况下，通过简化需求、分步实现来完成一个相对完整的项目。

**能跑，就是胜利！**

## 项目结构

```
Mini-Chat-Bar/
├── ccb/                    # 前端项目 (Vue.js + Electron)
│   ├── src/               # Vue.js 源代码
│   ├── public/            # 静态资源
│   ├── main.js            # Electron 主进程
│   ├── package.json       # 前端依赖
│   └── vite.config.js     # Vite 配置
├── server/                # 后端项目 (Node.js + Express)
│   ├── models/            # 数据模型
│   ├── routes/            # API 路由
│   ├── sockets/           # WebSocket 处理
│   ├── config/            # 数据库配置
│   ├── server.js          # 服务器入口
│   └── package.json       # 后端依赖
└── docs/                  # 项目文档
```

## 技术栈

### 前端
- Vue.js 3 - 渐进式前端框架
- Vite - 新一代前端构建工具
- Pinia - Vue 状态管理
- Vue Router - 路由管理
- Socket.IO Client - WebSocket 客户端
- Electron - 桌面应用框架

### 后端
- Node.js - JavaScript 运行时
- Express.js - Web 应用框架
- Socket.IO - 实时通信引擎
- MongoDB - NoSQL 数据库
- Mongoose - MongoDB ODM
- JWT - 身份认证
- Passport - 第三方登录

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- MongoDB >= 5.0
- npm >= 8.0.0

### 后端启动

```bash
# 进入后端目录
cd server

# 安装依赖
npm install

# 配置环境变量（创建 .env 文件）
# PORT=3000
# MONGO_URL=mongodb://localhost:27017/ccb

# 启动服务器（开发模式）
nodemon server.js
```

### 前端启动

```bash
# 进入前端目录
cd ccb

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

访问 http://localhost:5173 即可使用 Web 版本。

### Electron 桌面应用

```bash
# 在前端目录下
cd ccb

# 开发模式启动
npm run electron-dev

# 打包应用
npm run dist
```

详细的 Electron 配置说明请参考 [README-Electron.md](./README-Electron.md)

## 更多文档

- [Electron 桌面应用配置指南](./README-Electron.md)
- [GitHub 项目说明](./README-GitHub.md)

## 许可证

MIT License
